#001-为什么AI只会说正确的废话
最近看了个“播客-文本-skill-使用skill”的案例，是把32万字的播客本身蒸馏为skill,如果有用户想要更加深入了解有关内容，可以和配置了播客skill的AI对话。
链接：https://x.com/PMbackttfuture/status/2050036759115833587?s=19
虽然说skill本身由于其开源以及可复制性造成收费商业模式比较难做，但这个作为内容本身的附加产品不失为一种价值增值的方式。
LLM训练的本质是next token prediction，意思是：每一步输出概率最高的token，那么这就意味着说概率最高意味着最多人会认同，也就是consensus，所谓共识。
同样的模型，同样的工具，同样的 prompt，有的 agent 只能写出consensus、讲正确的废话；有的agent却能压缩出一句真正有立场的话、一句顶一万句。
差别往往不在模型本身，而在背后有没有一套稳定的判断框架。
这需要我们关注的就不是“如何把更多记忆喂给 agent”，而是“如何把 agent 设计成必须形成并检验判断”。
我们可以把agent蒸馏为skill，把某类问题的处理方式、工作流结构、判断逻辑与输出标准固化为一种可复用能力，然后在Codex或Agent系统中调用，通过工作流的编排，把模型能力发挥到想不到的高度。
很多真正强的系统，是从模型的反效果开始反向设计：
模型容易模糊表达，就强制明确回答；
模型容易漂移，就增加状态约束；
模型容易遗忘，就建立长期记忆；
模型容易泛化空谈，就强制引用上下文；
模型容易输出正确废话，就加入结果校验。
从这个角度看，Agent 并不是单纯在“使用模型”，而是在通过工程化与系统设计去不断驯化模型，而这可能才是AI工程真正开始变得有意思的地方，也是我们从使用者蜕变为构建者的关键所在。
